services:
  app:
    build: .
    container_name: ka-excavation
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - webhost

networks:
  webhost:
    external: true
